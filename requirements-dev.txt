# What the test suite needs on top of requirements.txt.
#
# httpx is here for one reason: several checks are about whether a ROUTE
# remembered to call something -- that a render refuses an asset address
# pointing at this machine, that no cross-origin header is handed out -- and
# a direct call to the function underneath cannot tell you that. Those tests
# drive the real app through fastapi's TestClient, which is httpx.
-r requirements.txt
pytest>=8.0
httpx>=0.27
